 "use client";
import Link from "next/link";import {LogOut} from "lucide-react";import {useEffect,useState} from "react";import {createClient} from "../../lib/supabase/client";
export default function DashboardPage(){const supabase=createClient();const[name,setName]=useState("Student");useEffect(()=>{supabase.auth.getUser().then(({data})=>{if(!data.user){window.location.href="/login";return}supabase.from("profiles").select("full_name").eq("id",data.user.id).single().then(({data:p})=>{if(p?.full_name)setName(p.full_name)})})},[supabase]);async function logout(){await supabase.auth.signOut();window.location.href="/"}return <main style={{minHeight:"100vh",padding:"30px",fontFamily:"Arial",background:"#fffaf7"}}><div style={{maxWidth:900,margin:"0 auto"}}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><Link href="/" className="landing-brand"><span className="brand-mark">🎓</span><span>Campus<span>Mind</span></span></Link><button onClick={logout} style={{border:0,background:"transparent",cursor:"pointer"}}><LogOut size={18}/></button></div><section style={{marginTop:60}}><p style={{color:"#ff5a16",fontWeight:800,fontSize:12}}>DASHBOARD</p><h1>Welcome, {name.split(" ")[0]}.</h1><p>Your CampusMind workspace is ready.</p></section></div></main>}
